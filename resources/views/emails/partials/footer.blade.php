<br>
<br>
<hr>
<br>
<table>
    <tr>
        <td>
            <a href="{{ env('SPA_URL') }}">
                <img src="{{ env('APP_URL') }}/{{ env('APP_LOGO') }}" alt="{{ env('APP_NAME') }}">
            </a>
        </td>
    </tr>
</table>
